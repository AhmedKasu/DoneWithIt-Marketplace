import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    appBg: {
      main: '#eceff1',
      navbar: '#ffffff',
      sidebar: '#ffffff',
    },
  },
});

export default theme;
